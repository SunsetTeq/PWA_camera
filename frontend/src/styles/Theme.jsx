import { createTheme } from '@mui/material/styles';

const theme = createTheme({
    colorSchemes: {
        dark: true,
    },
    palette: {
        primary: {
          main: '#a5d6a7',
        },
        secondary: {
          main: '#18ffff',
        },
      },
});

export default theme;